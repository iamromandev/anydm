# Port the API listens on (Bun reads PORT automatically, default 3000)
PORT=3000

# Runtime environment (dev | prod)
ENV=dev

# Comma-separated CORS origins allowed to call the API (UI dev server)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
