{
    "name": "@anydm/ui",
    "type": "module",
    "private": true,
    "scripts": {
        "clean": "rm -rf dist",
        "dev": "bun run clean && bun build src/main.tsx --outdir dist && bun run src/index.html",
        "build": "bun build src/main.tsx --outdir dist --public-path /"
    },
    "dependencies": {
        "react": "^18.3.1",
        "react-dom": "^18.3.1"
    },
    "devDependencies": {
        "@types/react": "^19.2.2",
        "@types/react-dom": "^19.2.2",
        "typescript": "^5.9.3"
    }
}